# manasPlanner (Frontend)

PyQt-based mission planner UI mock (layout only — no backend + map placeholder).

## Run

### 1) Install deps

Use the workspace virtualenv at `.venv`.

```bash
/home/parth/Desktop/Nidar-gui/.venv/bin/python -m pip install -r requirements.txt
```

### 2) Start the UI

```bash
/home/parth/Desktop/Nidar-gui/.venv/bin/python -m mission_planner
```

## Notes

- The map area is currently a placeholder widget.
- Drone statuses are currently static labels (easy to wire later).
# Manas-Planner
